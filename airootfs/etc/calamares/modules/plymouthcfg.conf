---
# Plymouth splash screen configuration
# Applied after users and displaymanager modules

# Theme to set on the installed system
theme: bgrt

# Whether to show plymouth on the installed system at all
# Set false if you want to skip it silently (acts as a no-op)
# Set true to configure /etc/plymouth/plymouthd.conf
true:
