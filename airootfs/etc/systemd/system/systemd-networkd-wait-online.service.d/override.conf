[Service]
# Prevent long boot delays when no network is available (e.g., live ISO without ethernet)
# Consider any interface (including lo) as sufficient, and timeout after 10s instead of 120s
ExecStart=
ExecStart=/usr/lib/systemd/systemd-networkd-wait-online --any --timeout=10
