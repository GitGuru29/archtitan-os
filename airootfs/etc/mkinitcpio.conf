# /etc/mkinitcpio.conf — standard installed system configuration
# Generated for ArchTitan — installed system (not live ISO)

MODULES=(i915 amdgpu nouveau virtio-gpu qxl bochs simpledrm)

BINARIES=()

FILES=()

HOOKS=(base udev autodetect microcode modconf kms keyboard keymap consolefont block filesystems fsck)

COMPRESSION="zstd"
COMPRESSION_OPTIONS=(-3)
