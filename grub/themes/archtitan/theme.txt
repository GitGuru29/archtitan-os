# ─────────────────────────────────────────────────────────────────────────────
# ArchTitan OS — Premium GRUB Theme
# Layout: Left-aligned menu panel, right side open for sphere artwork
# Background: 1920x1080 dark navy with holographic globe (right side)
# ─────────────────────────────────────────────────────────────────────────────

title-text:                  ""
desktop-image:               "splash.png"
desktop-image-scale-method:  "crop"
desktop-color:               "#080C1A"
terminal-font:               "Unifont Regular 14"

# ── OS Boot Menu (left panel, matching reference) ─────────────────────────────
+ boot_menu {
    left                     = 4%
    top                      = 30%
    width                    = 32%
    height                   = 45%

    # Text colors
    item_color               = "#8B9EC7"
    selected_item_color      = "#FFFFFF"

    # Row sizing
    item_height              = 42
    item_padding             = 16
    item_spacing             = 6
    item_font                = "Unifont Regular 14"
    selected_item_font       = "Unifont Regular 16"

    item_align               = "left"

    # Cyan-blue selection highlight (9-slice tile)
    selected_item_pixmap_style = "select_*.png"

    # Semi-transparent dark menu frame
    menu_pixmap_style        = "menu_*.png"

    icon_width               = 0
    icon_height              = 0
    scrollbar                = false
}

# ── "ArchTitan OS" title label ────────────────────────────────────────────────
+ label {
    left   = 4%
    top    = 23%
    width  = 32%
    height = 30
    text   = "ArchTitan OS"
    color  = "#60A5FA"
    font   = "Unifont Regular 20"
    align  = "left"
}

# ── Divider line under title ──────────────────────────────────────────────────
+ label {
    left   = 4%
    top    = 27%
    width  = 32%
    height = 2
    text   = "────────────────────────────"
    color  = "#1E3A5F"
    font   = "Unifont Regular 12"
    align  = "left"
}

# ── Countdown progress bar ────────────────────────────────────────────────────
+ progress_bar {
    id           = "__timeout__"
    left         = 4%
    top          = 78%
    width        = 32%
    height       = 3
    bg_color     = "#0F1729"
    fg_color     = "#3B82F6"
    border_color = "#1E3A5F"
    text_color   = "#60A5FA"
    text         = ""
    font         = "Unifont Regular 12"
}

# ── Timeout text label ────────────────────────────────────────────────────────
+ label {
    left   = 4%
    top    = 81%
    width  = 32%
    height = 22
    text   = "@TIMEOUT_NOTIFICATION_LONG@"
    color  = "#3B5480"
    font   = "Unifont Regular 12"
    align  = "left"
}

# ── Bottom hint ───────────────────────────────────────────────────────────────
+ label {
    left   = 4%
    top    = 93%
    width  = 32%
    height = 20
    text   = "Press 'e' to edit  |  'c' for console"
    color  = "#253554"
    font   = "Unifont Regular 11"
    align  = "left"
}
